# vim: ft=yaml
#
# Set default values.
---
values:
  lookup:
    pkg:
      name: git
    paths:
      confdir: ''
      conffile: '.gitconfig'
      xdg_dirname: 'git'
      xdg_conffile: 'config'
    rootgroup: root
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
